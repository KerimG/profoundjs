{
    // copy this into the .vscode/ folder
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Profound.js",
            "program": "${workspaceFolder}\\start.js",
            "args": [
                "--no-worker"
            ]
        }
    ]
}